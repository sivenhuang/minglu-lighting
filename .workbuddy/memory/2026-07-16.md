# 2026-07-16

## 恢复定时任务：每周 SEO 博客发布
- 原"每周博客发布"自动化此前已被软删除（automation list 为空）。
- 依据 2026-07-14 记忆 + 项目文件重建，新建自动化：
  - ID: `automation-1784175970652`
  - 名称: `Minglu Lighting 每周 SEO 博客发布`
  - 周期: 每周二（`FREQ=WEEKLY;BYDAY=TU`，原运行日是周二 09:00）
  - 状态: ACTIVE；工作目录 E:\WorkBuddy\minglu-lighting
  - 流程: 选题(参考 google-ads-strategy.md 关键词，避开已发10篇) → 写1500-2000词英文SEO博客 → 生成 blog/<slug>.html(复制 solar-vs-grid-street-light-5-year-tco.html 模板) → 在 blog.html 顶部插入卡片 → sitemap.xml 插入条目 → git commit & push origin main
- 注意：rrule 无法指定具体时刻，下次运行默认时间由系统决定；用户如需锁定 09:00 可再调整。

## 全盘 SEO & 性能审计（深度分析）
- 产出报告：`SEO-性能审计报告-2026-07-16.md`
- 🔴 高危：① GA4 迁移从未落地——全站129页仍用旧ID G-94KWTC5HK4，0页用新ID G-543007082（记忆说已迁移但实际未执行）；② 99产品页 aggregateRating 全雷同 4.8/127（疑似假评，有手动惩罚风险）；③ 99产品 price:"0"（无效Offer）；④ sitemap.xml 含26个重复产品URL。
- 🟡 中等：标题模板bug("Solar Solar X")+4重复标题/6重复描述；产品页正文仅~225词(偏薄)；静态分类页内链弱(产品面包屑指向 products.html?category= 而非静态 category/*.html)；非首页缺critical CSS(仅index有)；product.html孤儿模板无canonical。
- 🟢 性能：产品图片150MB、88张>500KB、24张>1MB、最大4.21MB(头号瓶颈)；products-data.js 192KB；usimg.bjyyb.net 无效预连接。已做对：结构化数据覆盖广、alt 2482/2483、懒加载2123张、canonical 128/129、字体异步加载。
- 建议优先修A组4项(脚本化安全替换)，再处理图片压缩(ROI最高)与内容增厚。

## A组4项已修复并部署（13:48-13:55）
- 提交 `2d82941` 推送到 main（130文件变更，Cloudflare Pages 自动部署）。
- A1 GA4：全站129页 G-94KWTC5HK4 → G-543007082（新属性正式接入，旧属性停用）。
- A2 虚假评价：99产品页 aggregateRating(4.8/127) 全移除。
- A3 价格：99产品页 offers 移除 price:"0"/priceValidUntil/priceCurrency，保留 availability 等。
- A4 sitemap：153→127条（去除26个重复product URL），XML 校验合法。
- 验证：产品 JSON-LD 全量解析 0 失败；git 因 dubious ownership 已加 safe.directory 例外。
- 注：.workbuddy 未被 gitignore，提交时精确暂存仅 html+sitemap，避免混入项目数据。
- 待办(B/C组)：图片压缩(150MB)、标题模板bug、内容增厚、静态分类页内链、critical CSS、products-data.js 按需加载、usimg无效预连接、product.html noindex。

## B组图片压缩已完成并部署（14:18-14:38）
- 提交 `6c907ff` 推送到 main（2d82941..6c907ff），Cloudflare Pages 自动部署中。
- 策略：全站 JPG+PNG 原地压缩，保持文件名/路径/扩展名完全不变（HTML 零改动，移动端响应式适配零影响）；长边 >1600 的缩放到 1600；JPG quality 82；PNG 仅 optimize；WebP 因已是高效格式且二次压缩多数反变大，已安全跳过。
- 结果：JPG+PNG 157.0MB → 108.8MB（省 48.2MB / 30.7%）；全站 `assets/images` 192MB → 144MB（省 25%）。
- 文件处理：346 个文件 → 316 个覆盖、30 个自动跳过（压缩后未更小）、17 个缩放、0 错误。
- 验证：未改任何 HTML；抽样 `assets/images/products/solar-garden-light-sg485-4.jpg`（原 3.3MB → 93.8KB）视觉正常；`.tmp_compress` 临时文件残留 0。
- 待办（C 组优先级）：标题模板 bug（如 "Solar Solar Street Light"）、产品页内容增厚（当前仅 ~225 词）、静态分类页内链强化、非首页 critical CSS、products-data.js 按需加载、usimg.bjyyb.net 无效预连接清理、product.html 孤儿页 noindex/canonical。用户如确认可继续推进。

## C组 SEO 优化已全部完成并部署（16:59-17:30）
- 提交 `17250a1` 推送到 main（6c907ff..17250a1，129 文件变更，Cloudflare Pages 自动部署）。
- **C1 标题模板 bug**：99 产品页标题由 `{名} - Minglu Lighting | Solar Solar {分类} Manufacturer` 改为 `{名} | Minglu Lighting`，"Solar Solar" 双词残留 = 0；同步 og:title/twitter:title；修正 1 个 JSON-LD description 占位坏值("Subscribe to get the latest...")。
  - 关键发现：C1 修复后剩余 4 组重复标题 = **19 个真正的重复 listing 页**（Solar Lawn Light×6、Solar Pillar Light×9、Solar street light×2、Solar powered led street light×2）。其中 p77/p82/p84 图片目录同为 SG-6、p1/p2 同 TR408 —— 这些是同一产品的重复页面，SKU 去重无效，正确解法是 **合并 canonical/301**（需用户决定保留哪个 URL，未自动执行）。
- **C2 无效预连接**：全站 129 文件删除 `<link rel="preconnect" href="https://usimg.bjyyb.net">`，残留 = 0。
- **C3 静态分类页内链**：99 产品页面包屑/页脚分类链接 + JSON-LD 面包屑 URL 由 `products.html?category=X` 改为静态 `category/X.html`（slug 与 12 个分类页 1:1 对应，全部有效），动态 `?category=` 残留 = 0。
- **C4 critical CSS**：将首页 inline critical CSS（39 行、无 url() 外部引用）注入其余 128 页 `<head>`（在 stylesheet 链接之前），含 `<style>` 页面 129/129。同套 CSS，移动端零风险。
- **C5 内容增厚**：99 产品页在 Related Products 前插入结构化区块（Overview / Typical Applications 按分类定制 / Why Choose Minglu Lighting），正文可见词数 ~225 → 290-335（约 +50%）；结构校验 0 异常。
- 全部脚本化，HTML 结构零破坏（style/head/section 标签平衡校验通过），精确暂存仅 129 个 html，未混入 .workbuddy。
- 遗留待办：① 19 个重复 listing 页合并 canonical（用户决策）；② products-data.js 192KB 按需加载 + CSS 压缩（可开 Cloudflare Auto-Minify）；③ product.html 孤儿模板加 noindex/canonical。

## 重复产品页合并已完成并部署（18:25-18:55）
- 提交 `5c0326f` 推送到 main（17250a1..5c0326f，19 文件变更，Cloudflare Pages 自动部署）。
- **重要认知修正**：原以为 19 页是"同款不同 listing"，深入比对发现它们**图片集/features 实际不同**（如 p93 是 8W、其余 5W；p1/p2 features 完全不同）。因此正确解法是 **canonical 合并（保留主URL）而非差异化标题**——差异化会把同一产品拆成多个假页面。
- **合并方案**（14 个重复页 → 5 个主 URL）：
  - Solar Lawn Light(SG-6) 6页 → 主 `p78`（图片最多=最完整）
  - Solar Pillar Light 5W 8页 → 主 `p86`；**p93 是 8W 独立款**，标题改为 "Solar Pillar Light 8W" 单独保留
  - Solar street light 2页 → 主 `p1`；Solar powered led street light 2页 → 主 `p6`
- **执行动作**：① 14 个重复页加 `<link rel="canonical">` 指向主 URL；② 同步将页内 og:url / JSON-LD url / 面包屑自引用也改为主 URL（4处/页）；③ sitemap.xml 99→85 产品 URL（删除 14 重复，XML 合法）；④ 3 个分类页删除重复 product-card 卡片 + 清理 ItemList（草坪灯9→4卡、柱状灯9→2卡、路灯53→49卡），柱状灯分类页 p93 卡片标题改 8W；⑤ numberOfItems 同步更新。
- **安全校验**：全站无其它页再指向 14 个重复页（仅自身引用）；3 个分类页 div 开闭差值与原始一致（42/39、33/30、267/264，原差均为3，未引入新不平衡）；viewport/critical CSS 完整（移动端零影响）；JSON-LD 全量 0 解析失败。
- **工具教训**：用 `<div class="product-card">` 字符串 split 删除卡片块是平衡安全的（每个卡片块自带闭合标签），但需警惕"被删的最后一个卡片携带了外层容器闭合标签"导致失衡——本次因原始文件本就有 3 个 `<div` 不平衡差值，删除后差值不变故安全；今后做块删除前应先 baseline 比对 div 平衡。
- **新发现（未处理，待用户决策）**：`category/solar-street-light.html` 把同一批 26 个产品**列了两遍**（53 张卡片=26 唯一+26 重复），属分类页自身复制粘贴重复，是独立于 19 页合并的另一个 bug，已标记待处理。
- 下一步可选：① 修复路灯分类页重复列表；② products-data.js 按需加载 + Cloudflare Auto-Minify；③ product.html 孤儿页 noindex。
